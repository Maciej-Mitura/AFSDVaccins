# Phase 35H1 — Release checkpoint

Controlled release preparation for Phase 35 (visual redesign, notification copy,
localisation, order History, courier analytics demo generator, presentation
docs, i18n workflow).

**Do not treat unchecked boxes as passed.** Items marked _not performed_ must
be completed after deployment approval.

| Field                   | Value                                                                       |
| ----------------------- | --------------------------------------------------------------------------- |
| Checkpoint date/time    | 2026-07-29 (Europe/Brussels, ~03:50)                                        |
| Intended release commit | `51e5dfa1887a120f5c574288e5562732dd0c0186`                                  |
| Branch                  | `develop` (clean, synced with `origin/develop`)                             |
| Checks performed by     | Local release validation (agent-assisted); production smoke TBD by operator |
| Deploy executed?        | **No** — awaiting explicit approval                                         |
| Analytics seed applied? | **No** — dry-run only                                                       |

---

## 1. Git / release readiness

| Check                          | Result                                                       |
| ------------------------------ | ------------------------------------------------------------ |
| Working tree                   | Clean                                                        |
| Branch vs origin               | `develop` = `origin/develop` (0 ahead / 0 behind)            |
| Merge/rebase state             | None                                                         |
| Untracked files                | None                                                         |
| Accidental credentials in tree | None observed in tracked paths                               |
| Audit artefacts                | Tracked; not dirty; `audit:i18n:check` does not rewrite them |
| Release commit                 | `51e5dfa1887a120f5c574288e5562732dd0c0186`                   |

### CI (GitHub Actions, HEAD `51e5dfa`)

| Workflow         | Conclusion                                                                                         |
| ---------------- | -------------------------------------------------------------------------------------------------- |
| CI PWA           | success                                                                                            |
| CI Playwright    | **failure** (step: Playwright browser E2E)                                                         |
| CI API / API E2E | last green seen on nearby SHA `6fd6724` (not re-run on HEAD; no API commits after Railway SUCCESS) |

Railway auto-deploys after `d35d242` were **SKIPPED** with reason `CI check suite failed` (Playwright). That does **not** block a manual Firebase Hosting deploy of the PWA.

---

## 2. Change-layer classification

Compared intended release `51e5dfa` with:

- **Railway (live):** SUCCESS deployment commit `d35d242` (“idk”), 2026-07-28T22:44:19Z; 1 replica; sleep disabled; health OK.
- **Firebase Hosting (live):** served asset hash differs from local production build (`index-o7s1y_fR.js` hosted vs `index-IRMyXq6N.js` local).

### Layers in Phase 35 window (order-history → HEAD)

| Layer                   | Changed?                                 | Notes                                                    |
| ----------------------- | ---------------------------------------- | -------------------------------------------------------- |
| PWA-only                | **Yes (majority after `d35d242`)**       | Redesign, History UI, i18n, a11y polish                  |
| API-only                | **Yes historically; already on Railway** | Order history, notification copy, analytics demo scripts |
| Shared packages/types   | Build-time codegen only                  | Regenerated by `build:pwa:production`                    |
| Infrastructure/config   | No Phase 35 delta requiring redeploy     |                                                          |
| Documentation/test-only | Yes                                      | Presentation showcase, i18n audit docs                   |

### Deployment decisions

| Question                               | Answer                                                                                                                                                          |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Firebase Hosting deploy required?      | **Yes**                                                                                                                                                         |
| Railway deploy required?               | **No** for this release (live API already includes `orderHistory`, `courierPerformanceAnalytics`, notification-copy commit era; no API commits after `d35d242`) |
| Shared type regeneration required?     | Covered by `npm run build:pwa:production`                                                                                                                       |
| API schema compatibility risk?         | **Low** — History UI uses existing production `orderHistory`; no incompatible schema cut planned                                                                |
| Environment-variable changes required? | **No** for PWA-only Hosting release (Railway vars already set; see §4)                                                                                          |

---

## 3. Local validation results

### Scope rationale

Focused on Phase 35 surfaces + production gates. Skipped full slow unrelated suites where CI/local already cover unchanged areas. Full API Jest ran because npm did not forward a path filter (still green).

| Command                                                                            | Result                                                                                    |
| ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `npm run audit:i18n:check`                                                         | **PASS** (parity OK; no missing keys; snapshots not compared)                             |
| `npm run build:pwa:production`                                                     | **PASS** (env validation + build). Re-run after Playwright so `dist/` is production again |
| `npm run build:api`                                                                | **PASS**                                                                                  |
| API Jest (workspace suite)                                                         | **PASS** — 127 suites, 968 tests (2 skipped)                                              |
| Focused PWA Vitest (10 files / admin·apotheker·bezorger·i18n·a11y·prod-env)        | **PASS** — 66 tests                                                                       |
| Playwright: `auth`, `role-isolation`, `admin`, `apotheker-order`, `bezorger-route` | **PASS** — 15/15                                                                          |
| `npm run validate:production-readiness`                                            | **PASS** — 10/10                                                                          |
| `npm run test:docker:safety`                                                       | **PASS** — 11/11                                                                          |

**Note:** Playwright preview rebuilds `packages/pwa/dist`. Always re-run `npm run build:pwa:production` immediately before `firebase deploy --only hosting`.

---

## 4. Environment readiness (names/status only — no secret values)

### PWA (`packages/pwa/.env.production.local`)

| Item                                    | Status                                                 |
| --------------------------------------- | ------------------------------------------------------ |
| `VITE_BACKEND_URL`                      | PRESENT (HTTPS GraphQL on Railway host)                |
| `VITE_BACKEND_WS_URL`                   | PRESENT (WSS GraphQL on Railway host)                  |
| Firebase web config (`VITE_FIREBASE_*`) | PRESENT                                                |
| `VITE_WEB_PUSH_ENABLED`                 | PRESENT                                                |
| `VITE_WEB_PUSH_VAPID_PUBLIC_KEY`        | PRESENT                                                |
| `VITE_E2E_AUTH_BYPASS`                  | **MISSING** (correct for production)                   |
| Firebase Hosting project                | Active alias `maciejafsdvaccin` (`firebase use`)       |
| `.firebaserc` in repo                   | Absent (gitignored pattern); CLI still selects project |

### Railway production (variable presence / shape)

| Item                                               | Status                                  |
| -------------------------------------------------- | --------------------------------------- |
| `NODE_ENV`                                         | production                              |
| `URL_FRONTEND`                                     | HTTPS host matches Firebase Hosting     |
| `TRUST_PROXY`                                      | `1`                                     |
| Replicas                                           | **1** (`numReplicas=1`)                 |
| Sleep / serverless                                 | **disabled** (`sleepApplication=false`) |
| `DB_HOST` / `DB_NAME`                              | PRESENT                                 |
| `FIREBASE_SERVICE_ACCOUNT_JSON`                    | PRESENT                                 |
| Azure Blob / Vision / Speech                       | PRESENT; transcription enabled          |
| `WEB_PUSH_SUBJECT`                                 | PRESENT (`mailto:` shape OK)            |
| VAPID public/private                               | PRESENT                                 |
| `DELIVERY_QR_SIGNING_SECRET`                       | PRESENT                                 |
| `ALLOW_E2E_AUTH_BYPASS`                            | `false`                                 |
| `ALLOW_DATABASE_SEED` / `ALLOW_DATABASE_BOOTSTRAP` | `false`                                 |
| `PUSH_PROVIDER`                                    | webpush                                 |

Local `packages/api/.env` is development/local Mongo — not used as the production source of truth.

---

## 5. Deployment order (approved path for this release)

API compatibility unchanged → **PWA-only** sequence:

1. Verify Railway health (already OK at checkpoint): `GET https://afsdvaccins-production.up.railway.app/health`
2. Ensure production `dist` is fresh: `npm run build:pwa:production`
3. Deploy Firebase Hosting (**approval required**): `firebase deploy --only hosting`
4. Verify Hosting serves new asset hash / SW update
5. Smoke-test all roles (matrix below)

If a future incompatible API change appears:

1. Fix CI Playwright (or override Railway skip policy deliberately)
2. Deploy API first (`railway up` / dashboard redeploy of intended commit)
3. Verify `/health` + GraphQL
4. Deploy PWA
5. Smoke-test

---

## 6. Deployed URLs (platform names — no secrets)

| Platform               | URL                                                           |
| ---------------------- | ------------------------------------------------------------- |
| PWA (Firebase Hosting) | `https://maciejafsdvaccin.web.app` (also `*.firebaseapp.com`) |
| API (Railway)          | `https://afsdvaccins-production.up.railway.app`               |
| GraphQL HTTP/WS        | `/graphql` on the Railway host                                |

---

## 7. Analytics demo-data

### Dry-run results

| Target                                                                 | Result                                                                                                              |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Local (`NODE_ENV=development`, local Mongo, `DB_NAME=vaccin-delivery`) | Dry-run OK — planned ~10 couriers, 162 routes, 399 stops, 399 orders                                                |
| Railway-injected env (production-like)                                 | Dry-run OK — same planned counts; **mutation would require** `CONFIRM_ANALYTICS_DEMO_DATA=SEED_ANALYTICS_DEMO_DATA` |

### Safety confirmations

- Default = dry-run; `--apply` / `--cleanup` explicit
- Deterministic IDs (`a35c00…` prefix family)
- Presentation users protected (docs + script contract)
- Cleanup: `--cleanup` only for generated markers
- Production guard: confirmation phrase required

### Recommendation

**Do not apply to production yet.** First open ADMIN → Courier analytics on live data after PWA deploy. Seed only if the dashboard is too sparse for presentation and mixing generated “Courier Stat NN” rows with real couriers is intentional. Require explicit approval + confirmation env before `--apply`.

Cleanup (only after approved apply):

```bash
cd packages/api
# with production gates set deliberately
npx ts-node --transpile-only scripts/seed-courier-analytics-demo.ts --cleanup
```

---

## 8. Production smoke-test matrix

Status at checkpoint: **prepared, not executed** (no live deploy of `51e5dfa` yet).

### Public / authentication

- [ ] PWA loads on Hosting URL
- [ ] New design / asset hash matches release build
- [ ] Register / login / forgot-password render
- [ ] Language switching (nl/en/es/zh)
- [ ] No E2E auth bypass behaviour
- [ ] Stale SW does not permanently pin old UI (see §9)

### ADMIN

- [ ] Dashboard
- [ ] Orders + actions
- [ ] History
- [ ] Route planning / templates
- [ ] Courier analytics + CSV
- [ ] Vaccines / Azure image controls
- [ ] Stock / stock history
- [ ] Notifications (friendly copy)
- [ ] Settings / profile
- [ ] Role protection

### APOTHEKER

- [ ] Dashboard
- [ ] Create order / my orders / History
- [ ] Vaccine catalogue / planned deliveries
- [ ] QR / manifest entry
- [ ] Notifications / profile

### BEZORGER

- [ ] Dashboard / today route / tomorrow preview
- [ ] Start/resume / arrival / offline cache
- [ ] QR scanner + manual fallback
- [ ] Voice report / city-level location
- [ ] Notifications / complete-cancelled states

Use presentation/test accounts only; avoid destructive production mutations.

---

## 9. Technical production checks

| Check                                                                           | Checkpoint status                                     |
| ------------------------------------------------------------------------------- | ----------------------------------------------------- |
| `GET /health`                                                                   | **PASS** (`status=ok`, `environment=production`)      |
| GraphQL health query                                                            | **PASS**                                              |
| GraphQL fields `orderHistory`, `courierPerformanceAnalytics`, `myNotifications` | Present (auth-gated `UNAUTHENTICATED`)                |
| Security headers on `/health`                                                   | CSP, XCTO, XFO, HSTS, CORP present                    |
| CORS / WS / Blob media / analytics UI                                           | **Not fully smoke-tested in browser this checkpoint** |
| Service worker / manifest                                                       | Hosting serves `sw.js` + manifest; post-deploy verify |
| API NetworkOnly in SW                                                           | Implemented in `packages/pwa/src/sw.ts`               |
| Console / i18n / 401 loops                                                      | Post-deploy browser pass required                     |

---

## 10. Service-worker update behaviour

Configured: `registerType: 'prompt'`; banner via `useAppUpdate` → `updateServiceWorker(true)`.

| Scenario                                   | Status                  |
| ------------------------------------------ | ----------------------- |
| Clean profile gets current build           | **Pending** post-deploy |
| Existing tab detects update + “Update now” | **Pending** post-deploy |
| Installed PWA updates after reopen         | **Pending** post-deploy |
| No infinite reload loop                    | **Pending** post-deploy |
| Old caches cleaned                         | **Pending** post-deploy |

**Defect triage rule:** if Update now is inert but hard reload / reopen yields the new asset hash → non-blocking; if neither path updates → release-blocking. Do not treat “clear all site data” as the only accepted fix.

---

## 11. Rollback

| Layer            | Method                                                                                                            |
| ---------------- | ----------------------------------------------------------------------------------------------------------------- |
| Firebase Hosting | `firebase hosting:clone` prior version **or** rebuild previous commit `dist` and `firebase deploy --only hosting` |
| Railway          | Redeploy previous SUCCESS deployment / commit (current live: `d35d242`)                                           |
| Database         | Analytics: `--cleanup` for deterministic demo rows only — **no** broad delete                                     |
| Service worker   | Ship corrective Hosting build with new precache hashes; avoid asking all users to clear storage                   |

---

## 12. Known limitations

- CI Playwright failing on `develop` HEAD → Railway skips newer API deploys (irrelevant for this PWA-only release, but must be fixed before the next API change).
- Production browser smoke + SW update matrix not yet executed.
- Analytics demo not applied; usefulness of live courier data not visually confirmed in this checkpoint.
- Local API `.env` is not Railway; do not confuse local dry-run DB with Atlas unless using `railway run`.

---

## 13. Commands requiring explicit approval

```bash
# 1) PWA production deploy (required for this release)
npm run build:pwa:production
firebase deploy --only hosting

# 2) API redeploy (NOT required for 35H1 unless API delta appears)
railway up
# or Railway dashboard: redeploy selected deployment/commit

# 3) Analytics demo apply (NOT recommended until dashboard review + approval)
# CONFIRM_ANALYTICS_DEMO_DATA=SEED_ANALYTICS_DEMO_DATA
npx ts-node --transpile-only packages/api/scripts/seed-courier-analytics-demo.ts --apply

# 4) Analytics demo cleanup (only after an approved apply)
npx ts-node --transpile-only packages/api/scripts/seed-courier-analytics-demo.ts --cleanup
```

---

## 14. Remaining release blockers

1. **Explicit approval** to run `firebase deploy --only hosting`
2. Fresh `npm run build:pwa:production` immediately before that deploy
3. Post-deploy smoke matrix (§8) and SW update checks (§10)
4. CI Playwright failure (soft for Hosting; hard for future Railway auto-deploys)
5. Analytics `--apply` decision deferred pending live dashboard review + separate approval
